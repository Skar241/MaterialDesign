<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:fab="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.saic.oscar.materialdesign.fragmentos.Home">

    <!-- TODO: Update blank fragment layout -->
    <TextView android:layout_width="match_parent" android:layout_height="match_parent"
        android:text="Home" />

    <com.getbase.floatingactionbutton.FloatingActionButton
        android:id="@+id/pink_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        fab:fab_icon="@drawable/ic_action_star"
        fab:fab_colorNormal="@color/accent_color"
        fab:fab_colorPressed="@color/accent_color"

        android:layout_alignParentBottom="true"
        android:layout_toLeftOf="@+id/pink"
        android:layout_toStartOf="@+id/pink"
        android:layout_gravity="right|bottom" />

</FrameLayout>
